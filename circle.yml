general:
  branches:
    only:
      - master
machine:
  python:
    version: 2.7.5
test:
  pre:
    - mkdir /home/static_cdn
    - python manage.py collectstatic --noinput
  override:
    - python manage.py test