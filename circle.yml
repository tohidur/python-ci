general:
  branches:
    only:
      - master
machine:
  python:
    version: 2.7.5
test:
  pre:
    - os.makedir(/home/static_cdn)
    - python manage.py collectstatic --noinput
  override:
    - python manage.py test